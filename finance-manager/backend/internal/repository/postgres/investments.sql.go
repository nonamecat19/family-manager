// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: investments.sql

package postgres

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createInvestment = `-- name: CreateInvestment :one
INSERT INTO investments (user_id, wallet_id, type, coin, amount, start_date, end_date, daily_reward, status)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
RETURNING id, user_id, wallet_id, type, coin, amount, start_date, end_date, daily_reward, status
`

type CreateInvestmentParams struct {
	UserID      pgtype.Int4    `json:"user_id"`
	WalletID    pgtype.Int4    `json:"wallet_id"`
	Type        string         `json:"type"`
	Coin        pgtype.Text    `json:"coin"`
	Amount      pgtype.Numeric `json:"amount"`
	StartDate   pgtype.Date    `json:"start_date"`
	EndDate     pgtype.Date    `json:"end_date"`
	DailyReward pgtype.Numeric `json:"daily_reward"`
	Status      string         `json:"status"`
}

func (q *Queries) CreateInvestment(ctx context.Context, arg CreateInvestmentParams) (Investment, error) {
	row := q.db.QueryRow(ctx, createInvestment,
		arg.UserID,
		arg.WalletID,
		arg.Type,
		arg.Coin,
		arg.Amount,
		arg.StartDate,
		arg.EndDate,
		arg.DailyReward,
		arg.Status,
	)
	var i Investment
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.WalletID,
		&i.Type,
		&i.Coin,
		&i.Amount,
		&i.StartDate,
		&i.EndDate,
		&i.DailyReward,
		&i.Status,
	)
	return i, err
}

const deleteInvestment = `-- name: DeleteInvestment :exec
DELETE FROM investments
WHERE id = $1
`

func (q *Queries) DeleteInvestment(ctx context.Context, id int32) error {
	_, err := q.db.Exec(ctx, deleteInvestment, id)
	return err
}

const getInvestmentByID = `-- name: GetInvestmentByID :one
SELECT id, user_id, wallet_id, type, coin, amount, start_date, end_date, daily_reward, status FROM investments
WHERE id = $1
`

func (q *Queries) GetInvestmentByID(ctx context.Context, id int32) (Investment, error) {
	row := q.db.QueryRow(ctx, getInvestmentByID, id)
	var i Investment
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.WalletID,
		&i.Type,
		&i.Coin,
		&i.Amount,
		&i.StartDate,
		&i.EndDate,
		&i.DailyReward,
		&i.Status,
	)
	return i, err
}

const getInvestmentsByUserID = `-- name: GetInvestmentsByUserID :many
SELECT id, user_id, wallet_id, type, coin, amount, start_date, end_date, daily_reward, status FROM investments
WHERE user_id = $1
ORDER BY start_date DESC
`

func (q *Queries) GetInvestmentsByUserID(ctx context.Context, userID pgtype.Int4) ([]Investment, error) {
	rows, err := q.db.Query(ctx, getInvestmentsByUserID, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Investment
	for rows.Next() {
		var i Investment
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.WalletID,
			&i.Type,
			&i.Coin,
			&i.Amount,
			&i.StartDate,
			&i.EndDate,
			&i.DailyReward,
			&i.Status,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateInvestment = `-- name: UpdateInvestment :one
UPDATE investments
SET wallet_id = $2, type = $3, coin = $4, amount = $5, start_date = $6, 
    end_date = $7, daily_reward = $8, status = $9
WHERE id = $1
RETURNING id, user_id, wallet_id, type, coin, amount, start_date, end_date, daily_reward, status
`

type UpdateInvestmentParams struct {
	ID          int32          `json:"id"`
	WalletID    pgtype.Int4    `json:"wallet_id"`
	Type        string         `json:"type"`
	Coin        pgtype.Text    `json:"coin"`
	Amount      pgtype.Numeric `json:"amount"`
	StartDate   pgtype.Date    `json:"start_date"`
	EndDate     pgtype.Date    `json:"end_date"`
	DailyReward pgtype.Numeric `json:"daily_reward"`
	Status      string         `json:"status"`
}

func (q *Queries) UpdateInvestment(ctx context.Context, arg UpdateInvestmentParams) (Investment, error) {
	row := q.db.QueryRow(ctx, updateInvestment,
		arg.ID,
		arg.WalletID,
		arg.Type,
		arg.Coin,
		arg.Amount,
		arg.StartDate,
		arg.EndDate,
		arg.DailyReward,
		arg.Status,
	)
	var i Investment
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.WalletID,
		&i.Type,
		&i.Coin,
		&i.Amount,
		&i.StartDate,
		&i.EndDate,
		&i.DailyReward,
		&i.Status,
	)
	return i, err
}
