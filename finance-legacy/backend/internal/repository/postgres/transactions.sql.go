// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: transactions.sql

package postgres

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createTransaction = `-- name: CreateTransaction :one
INSERT INTO transactions (wallet_id, type, amount, currency, description, category, executed_at)
VALUES ($1, $2, $3, $4, $5, $6, $7)
RETURNING id, wallet_id, type, amount, currency, description, category, created_at, executed_at
`

type CreateTransactionParams struct {
	WalletID    pgtype.Int4      `json:"wallet_id"`
	Type        string           `json:"type"`
	Amount      pgtype.Numeric   `json:"amount"`
	Currency    string           `json:"currency"`
	Description pgtype.Text      `json:"description"`
	Category    pgtype.Text      `json:"category"`
	ExecutedAt  pgtype.Timestamp `json:"executed_at"`
}

func (q *Queries) CreateTransaction(ctx context.Context, arg CreateTransactionParams) (Transaction, error) {
	row := q.db.QueryRow(ctx, createTransaction,
		arg.WalletID,
		arg.Type,
		arg.Amount,
		arg.Currency,
		arg.Description,
		arg.Category,
		arg.ExecutedAt,
	)
	var i Transaction
	err := row.Scan(
		&i.ID,
		&i.WalletID,
		&i.Type,
		&i.Amount,
		&i.Currency,
		&i.Description,
		&i.Category,
		&i.CreatedAt,
		&i.ExecutedAt,
	)
	return i, err
}

const deleteTransaction = `-- name: DeleteTransaction :exec
DELETE FROM transactions
WHERE id = $1
`

func (q *Queries) DeleteTransaction(ctx context.Context, id int32) error {
	_, err := q.db.Exec(ctx, deleteTransaction, id)
	return err
}

const getTransactionByID = `-- name: GetTransactionByID :one
SELECT id, wallet_id, type, amount, currency, description, category, created_at, executed_at FROM transactions
WHERE id = $1
`

func (q *Queries) GetTransactionByID(ctx context.Context, id int32) (Transaction, error) {
	row := q.db.QueryRow(ctx, getTransactionByID, id)
	var i Transaction
	err := row.Scan(
		&i.ID,
		&i.WalletID,
		&i.Type,
		&i.Amount,
		&i.Currency,
		&i.Description,
		&i.Category,
		&i.CreatedAt,
		&i.ExecutedAt,
	)
	return i, err
}

const getTransactionsByWalletID = `-- name: GetTransactionsByWalletID :many
SELECT id, wallet_id, type, amount, currency, description, category, created_at, executed_at FROM transactions
WHERE wallet_id = $1
  AND ($2::TIMESTAMP IS NULL OR executed_at >= $2)
  AND ($3::TIMESTAMP IS NULL OR executed_at <= $3)
ORDER BY executed_at DESC, created_at DESC
`

type GetTransactionsByWalletIDParams struct {
	WalletID pgtype.Int4      `json:"wallet_id"`
	Column2  pgtype.Timestamp `json:"column_2"`
	Column3  pgtype.Timestamp `json:"column_3"`
}

func (q *Queries) GetTransactionsByWalletID(ctx context.Context, arg GetTransactionsByWalletIDParams) ([]Transaction, error) {
	rows, err := q.db.Query(ctx, getTransactionsByWalletID, arg.WalletID, arg.Column2, arg.Column3)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Transaction
	for rows.Next() {
		var i Transaction
		if err := rows.Scan(
			&i.ID,
			&i.WalletID,
			&i.Type,
			&i.Amount,
			&i.Currency,
			&i.Description,
			&i.Category,
			&i.CreatedAt,
			&i.ExecutedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateTransaction = `-- name: UpdateTransaction :one
UPDATE transactions
SET type = $2, amount = $3, currency = $4, description = $5, category = $6, executed_at = $7
WHERE id = $1
RETURNING id, wallet_id, type, amount, currency, description, category, created_at, executed_at
`

type UpdateTransactionParams struct {
	ID          int32            `json:"id"`
	Type        string           `json:"type"`
	Amount      pgtype.Numeric   `json:"amount"`
	Currency    string           `json:"currency"`
	Description pgtype.Text      `json:"description"`
	Category    pgtype.Text      `json:"category"`
	ExecutedAt  pgtype.Timestamp `json:"executed_at"`
}

func (q *Queries) UpdateTransaction(ctx context.Context, arg UpdateTransactionParams) (Transaction, error) {
	row := q.db.QueryRow(ctx, updateTransaction,
		arg.ID,
		arg.Type,
		arg.Amount,
		arg.Currency,
		arg.Description,
		arg.Category,
		arg.ExecutedAt,
	)
	var i Transaction
	err := row.Scan(
		&i.ID,
		&i.WalletID,
		&i.Type,
		&i.Amount,
		&i.Currency,
		&i.Description,
		&i.Category,
		&i.CreatedAt,
		&i.ExecutedAt,
	)
	return i, err
}
