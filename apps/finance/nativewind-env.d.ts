/// <reference types="nativewind/types" />
// NativeWind adds `className` to React Native's components. Without this reference every
// styled element is a type error, so each package that writes JSX needs its own copy.
