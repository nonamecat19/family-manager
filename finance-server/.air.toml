root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ./cmd/api"
  bin = "tmp/main"
  include_ext = ["go", "sql", "yaml"]
  exclude_dir = ["tmp", "vendor", "node_modules"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
