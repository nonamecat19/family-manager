// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: tokens.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createRefreshToken = `-- name: CreateRefreshToken :one
INSERT INTO refresh_tokens (user_id, token_hash, chain_id, expires_at)
SELECT $1, $2, $3, $4
WHERE NOT EXISTS (
    SELECT 1 FROM refresh_tokens
    WHERE chain_id = $3 AND revoked_at IS NOT NULL
)
RETURNING id, user_id, token_hash, chain_id, used_at, revoked_at, expires_at, created_at
`

type CreateRefreshTokenParams struct {
	UserID    pgtype.UUID
	TokenHash string
	ChainID   pgtype.UUID
	ExpiresAt pgtype.Timestamptz
}

// The WHERE NOT EXISTS closes a race: a refresh that passed the "is this chain alive?" check
// can otherwise insert its successor a moment after a concurrent replay revoked the chain,
// resurrecting it. Inserting nothing returns no rows, which the handler treats as a refusal.
func (q *Queries) CreateRefreshToken(ctx context.Context, arg CreateRefreshTokenParams) (RefreshToken, error) {
	row := q.db.QueryRow(ctx, createRefreshToken,
		arg.UserID,
		arg.TokenHash,
		arg.ChainID,
		arg.ExpiresAt,
	)
	var i RefreshToken
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.TokenHash,
		&i.ChainID,
		&i.UsedAt,
		&i.RevokedAt,
		&i.ExpiresAt,
		&i.CreatedAt,
	)
	return i, err
}

const deleteExpiredRefreshTokens = `-- name: DeleteExpiredRefreshTokens :execrows
DELETE FROM refresh_tokens
WHERE expires_at < NOW()
`

func (q *Queries) DeleteExpiredRefreshTokens(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, deleteExpiredRefreshTokens)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getRefreshToken = `-- name: GetRefreshToken :one
SELECT id, user_id, token_hash, chain_id, used_at, revoked_at, expires_at, created_at FROM refresh_tokens
WHERE token_hash = $1
`

func (q *Queries) GetRefreshToken(ctx context.Context, tokenHash string) (RefreshToken, error) {
	row := q.db.QueryRow(ctx, getRefreshToken, tokenHash)
	var i RefreshToken
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.TokenHash,
		&i.ChainID,
		&i.UsedAt,
		&i.RevokedAt,
		&i.ExpiresAt,
		&i.CreatedAt,
	)
	return i, err
}

const markRefreshTokenUsed = `-- name: MarkRefreshTokenUsed :execrows
UPDATE refresh_tokens
SET used_at = NOW()
WHERE id = $1 AND used_at IS NULL AND revoked_at IS NULL
`

// Marks the token spent. The WHERE clause is the concurrency guard: two refreshes racing on
// the same token, only one updates a row, and the loser is treated as a replay.
func (q *Queries) MarkRefreshTokenUsed(ctx context.Context, id pgtype.UUID) (int64, error) {
	result, err := q.db.Exec(ctx, markRefreshTokenUsed, id)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const revokeAllForUser = `-- name: RevokeAllForUser :execrows
UPDATE refresh_tokens
SET revoked_at = NOW()
WHERE user_id = $1 AND revoked_at IS NULL
`

func (q *Queries) RevokeAllForUser(ctx context.Context, userID pgtype.UUID) (int64, error) {
	result, err := q.db.Exec(ctx, revokeAllForUser, userID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const revokeChain = `-- name: RevokeChain :execrows
UPDATE refresh_tokens
SET revoked_at = NOW()
WHERE chain_id = $1 AND revoked_at IS NULL
`

// Reuse of a spent token means it leaked: kill the whole rotation chain, not just this token,
// because the thief and the victim both hold descendants of it.
func (q *Queries) RevokeChain(ctx context.Context, chainID pgtype.UUID) (int64, error) {
	result, err := q.db.Exec(ctx, revokeChain, chainID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
