# Login flow: sign in with an existing account.
# Requires a running backend and a pre-registered user.
appId: dev.familymanager.recipes
name: "Auth: login"
tags:
  - auth
env:
  TEST_EMAIL: "testchef@example.com"
  TEST_PASSWORD: "testpass123"
---
- launchApp:
    clearState: true
- assertVisible: "Family Recipes"

- tapOn: "Email"
- inputText: ${TEST_EMAIL}
- tapOn: "Password"
- inputText: ${TEST_PASSWORD}

- tapOn: "Sign in"

# After login the app lands on Home (or onboarding if the account has no family yet).
- extendedWaitUntil:
    visible: "The family cookbook"
    timeout: 10000
