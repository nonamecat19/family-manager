{
  "name": "@fm/theme",
  "version": "0.0.0",
  "private": true,
  "description": "Design tokens and the light/dark colour scheme. The only place a colour literal is allowed to exist on the TypeScript side.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@fm/config": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^26.4.0"
  },
  "type": "module"
}
