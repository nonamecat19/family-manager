# No blank issues: the bug form's only mandatory fields are the two that make an issue
# actionable, and an empty issue is a conversation that starts by asking for them anyway.
blank_issues_enabled: false
