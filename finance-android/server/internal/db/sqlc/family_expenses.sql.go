// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: family_expenses.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getFamilyCategoryTotals = `-- name: GetFamilyCategoryTotals :many
SELECT
    e.category_id,
    c.name AS category_name,
    c.color AS category_color,
    c.icon AS category_icon,
    SUM(e.amount_cents)::BIGINT AS total_cents,
    COUNT(*)::INT AS expense_count
FROM expenses e
JOIN family_members fm ON fm.user_id = e.user_id
JOIN categories c ON c.id = e.category_id
WHERE fm.family_id = $1
  AND e.expense_date >= $2
  AND e.expense_date <= $3
GROUP BY e.category_id, c.name, c.color, c.icon
ORDER BY total_cents DESC
`

type GetFamilyCategoryTotalsParams struct {
	FamilyID      pgtype.UUID `json:"family_id"`
	ExpenseDate   pgtype.Date `json:"expense_date"`
	ExpenseDate_2 pgtype.Date `json:"expense_date_2"`
}

type GetFamilyCategoryTotalsRow struct {
	CategoryID    pgtype.UUID `json:"category_id"`
	CategoryName  string      `json:"category_name"`
	CategoryColor string      `json:"category_color"`
	CategoryIcon  string      `json:"category_icon"`
	TotalCents    int64       `json:"total_cents"`
	ExpenseCount  int32       `json:"expense_count"`
}

func (q *Queries) GetFamilyCategoryTotals(ctx context.Context, arg GetFamilyCategoryTotalsParams) ([]GetFamilyCategoryTotalsRow, error) {
	rows, err := q.db.Query(ctx, getFamilyCategoryTotals, arg.FamilyID, arg.ExpenseDate, arg.ExpenseDate_2)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetFamilyCategoryTotalsRow
	for rows.Next() {
		var i GetFamilyCategoryTotalsRow
		if err := rows.Scan(
			&i.CategoryID,
			&i.CategoryName,
			&i.CategoryColor,
			&i.CategoryIcon,
			&i.TotalCents,
			&i.ExpenseCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getFamilyExpenses = `-- name: GetFamilyExpenses :many
SELECT
    e.id,
    e.user_id,
    u.email AS user_email,
    e.category_id,
    c.name AS category_name,
    c.color AS category_color,
    c.icon AS category_icon,
    e.amount_cents,
    e.note,
    e.expense_date,
    e.created_at
FROM expenses e
JOIN family_members fm ON fm.user_id = e.user_id
JOIN users u ON u.id = e.user_id
JOIN categories c ON c.id = e.category_id
WHERE fm.family_id = $1
ORDER BY e.expense_date DESC, e.created_at DESC
LIMIT $2 OFFSET $3
`

type GetFamilyExpensesParams struct {
	FamilyID pgtype.UUID `json:"family_id"`
	Limit    int32       `json:"limit"`
	Offset   int32       `json:"offset"`
}

type GetFamilyExpensesRow struct {
	ID            pgtype.UUID        `json:"id"`
	UserID        pgtype.UUID        `json:"user_id"`
	UserEmail     string             `json:"user_email"`
	CategoryID    pgtype.UUID        `json:"category_id"`
	CategoryName  string             `json:"category_name"`
	CategoryColor string             `json:"category_color"`
	CategoryIcon  string             `json:"category_icon"`
	AmountCents   int64              `json:"amount_cents"`
	Note          string             `json:"note"`
	ExpenseDate   pgtype.Date        `json:"expense_date"`
	CreatedAt     pgtype.Timestamptz `json:"created_at"`
}

func (q *Queries) GetFamilyExpenses(ctx context.Context, arg GetFamilyExpensesParams) ([]GetFamilyExpensesRow, error) {
	rows, err := q.db.Query(ctx, getFamilyExpenses, arg.FamilyID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetFamilyExpensesRow
	for rows.Next() {
		var i GetFamilyExpensesRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.UserEmail,
			&i.CategoryID,
			&i.CategoryName,
			&i.CategoryColor,
			&i.CategoryIcon,
			&i.AmountCents,
			&i.Note,
			&i.ExpenseDate,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getFamilyMemberTotals = `-- name: GetFamilyMemberTotals :many
SELECT
    e.user_id,
    u.email AS user_email,
    SUM(e.amount_cents)::BIGINT AS total_cents,
    COUNT(*)::INT AS expense_count
FROM expenses e
JOIN family_members fm ON fm.user_id = e.user_id
JOIN users u ON u.id = e.user_id
WHERE fm.family_id = $1
  AND e.expense_date >= $2
  AND e.expense_date <= $3
GROUP BY e.user_id, u.email
ORDER BY total_cents DESC
`

type GetFamilyMemberTotalsParams struct {
	FamilyID      pgtype.UUID `json:"family_id"`
	ExpenseDate   pgtype.Date `json:"expense_date"`
	ExpenseDate_2 pgtype.Date `json:"expense_date_2"`
}

type GetFamilyMemberTotalsRow struct {
	UserID       pgtype.UUID `json:"user_id"`
	UserEmail    string      `json:"user_email"`
	TotalCents   int64       `json:"total_cents"`
	ExpenseCount int32       `json:"expense_count"`
}

func (q *Queries) GetFamilyMemberTotals(ctx context.Context, arg GetFamilyMemberTotalsParams) ([]GetFamilyMemberTotalsRow, error) {
	rows, err := q.db.Query(ctx, getFamilyMemberTotals, arg.FamilyID, arg.ExpenseDate, arg.ExpenseDate_2)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetFamilyMemberTotalsRow
	for rows.Next() {
		var i GetFamilyMemberTotalsRow
		if err := rows.Scan(
			&i.UserID,
			&i.UserEmail,
			&i.TotalCents,
			&i.ExpenseCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
