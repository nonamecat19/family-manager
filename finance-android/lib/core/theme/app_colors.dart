import 'package:flutter/material.dart';

/// App color constants.
abstract final class AppColors {
  /// Seed color for generating the Material 3 color scheme.
  static const Color seed = Colors.teal;
}
