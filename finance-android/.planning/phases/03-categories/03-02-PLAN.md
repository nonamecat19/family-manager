---
phase: 03-categories
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - app/lib/features/categories/data/models/category.dart
  - app/lib/features/categories/data/category_repository.dart
  - app/lib/features/categories/data/category_icons.dart
  - app/lib/features/categories/data/category_colors.dart
  - app/lib/features/categories/domain/category_state.dart
  - app/lib/providers/category_provider.dart
  - app/lib/features/categories/presentation/categories_screen.dart
  - app/lib/features/categories/presentation/category_form_screen.dart
  - app/lib/features/categories/presentation/widgets/category_chip.dart
  - app/lib/features/categories/presentation/widgets/category_tile.dart
  - app/lib/features/categories/presentation/widgets/icon_picker_sheet.dart
  - app/lib/features/categories/presentation/widgets/color_picker_sheet.dart
  - app/lib/features/settings/presentation/settings_screen.dart
  - app/lib/core/router/app_router.dart
  - app/test/features/categories/presentation/categories_screen_test.dart
  - app/test/features/categories/presentation/category_form_screen_test.dart
autonomous: false
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-05]

must_haves:
  truths:
    - "User can navigate to Settings > Categories and see their category list"
    - "User can create a category with name, icon (from grid picker), and color (from palette picker)"
    - "User can edit a category by tapping it -- form pre-fills with current values"
    - "User can delete a category via swipe-left with confirmation dialog"
    - "User can drag-to-reorder categories and order persists on server"
    - "User sees starter category prompt on first visit with Add/Skip options"
    - "Category chip widget (pill with icon + name) is reusable for future phases"
    - "Categories persist across sessions via backend API"
  artifacts:
    - path: "app/lib/features/categories/data/models/category.dart"
      provides: "Category model with fromJson/toJson"
      contains: "class Category"
    - path: "app/lib/features/categories/data/category_repository.dart"
      provides: "CategoryRepository with CRUD + reorder + bulk via Dio"
      contains: "class CategoryRepository"
    - path: "app/lib/features/categories/data/category_icons.dart"
      provides: "Map<String, IconData> of 30+ curated Material Icons"
      contains: "categoryIcons"
    - path: "app/lib/features/categories/data/category_colors.dart"
      provides: "List<Color> of 16 curated colors"
      contains: "categoryColors"
    - path: "app/lib/providers/category_provider.dart"
      provides: "CategoryNotifier StateNotifier + categoryStateProvider"
      contains: "class CategoryNotifier"
    - path: "app/lib/features/categories/presentation/categories_screen.dart"
      provides: "Categories list with ReorderableListView, Dismissible swipe-to-delete, starter prompt"
      contains: "class CategoriesScreen"
    - path: "app/lib/features/categories/presentation/category_form_screen.dart"
      provides: "Create/edit form with name field, icon picker, color picker"
      contains: "class CategoryFormScreen"
    - path: "app/lib/features/categories/presentation/widgets/category_chip.dart"
      provides: "Reusable colored pill widget for expense history and charts"
      contains: "class CategoryChip"
    - path: "app/lib/features/categories/presentation/widgets/icon_picker_sheet.dart"
      provides: "Bottom sheet with curated icon grid"
      contains: "showIconPicker"
    - path: "app/lib/features/categories/presentation/widgets/color_picker_sheet.dart"
      provides: "Bottom sheet with color palette circles"
      contains: "showColorPicker"
  key_links:
    - from: "app/lib/features/categories/data/category_repository.dart"
      to: "/api/v1/categories"
      via: "Dio HTTP client with auth interceptor"
      pattern: "dio\\.(get|post|put|delete).*categories"
    - from: "app/lib/providers/category_provider.dart"
      to: "app/lib/features/categories/data/category_repository.dart"
      via: "Constructor dependency injection"
      pattern: "CategoryRepository"
    - from: "app/lib/features/categories/presentation/categories_screen.dart"
      to: "app/lib/providers/category_provider.dart"
      via: "ref.watch(categoryStateProvider)"
      pattern: "categoryStateProvider"
    - from: "app/lib/features/settings/presentation/settings_screen.dart"
      to: "/settings/categories"
      via: "GoRouter navigation"
      pattern: "context\\.go.*categories"
    - from: "app/lib/core/router/app_router.dart"
      to: "app/lib/features/categories/presentation/categories_screen.dart"
      via: "GoRoute path registration"
      pattern: "/settings/categories"
---

<objective>
Flutter categories feature: data layer, state management, UI screens (list, form, pickers), routing, and widget tests.

Purpose: Delivers the complete user-facing category management experience -- creating, editing, deleting, and reordering categories with visual identity (icons and colors). Categories are the foundation for expense entry in Phase 4.
Output: Full categories feature with 6+ screens/widgets, Riverpod state management, 10+ widget tests, integrated into settings navigation.
</objective>

<execution_context>
@/home/nnc/.claude/get-shit-done/workflows/execute-plan.md
@/home/nnc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-categories/03-CONTEXT.md
@.planning/phases/03-categories/03-RESEARCH.md
@.planning/phases/03-categories/03-01-SUMMARY.md

<interfaces>
<!-- Existing patterns the executor needs to follow -->

From app/lib/providers/auth_provider.dart (StateNotifier pattern):
```dart
class AuthNotifier extends StateNotifier<AuthState> {
  AuthNotifier(this._repository, this._storage) : super(const AuthInitial());
  final AuthRepository _repository;
  final SecureStorageService _storage;
  // CRUD methods that set state...
}

final authStateProvider = StateNotifierProvider<AuthNotifier, AuthState>((ref) {
  final repository = ref.read(authRepositoryProvider);
  final storage = ref.read(secureStorageProvider);
  return AuthNotifier(repository, storage);
});
```

From app/lib/features/auth/domain/auth_state.dart (sealed state pattern):
```dart
sealed class AuthState { const AuthState(); }
class AuthInitial extends AuthState { const AuthInitial(); }
class AuthLoading extends AuthState { const AuthLoading(); }
class Authenticated extends AuthState { ... }
class Unauthenticated extends AuthState { const Unauthenticated(); }
class AuthError extends AuthState { ... }
```

From app/lib/core/network/api_client.dart (Dio provider):
```dart
final dioProvider = Provider<Dio>((ref) { ... });
```

From app/lib/core/router/app_router.dart (routing structure):
```dart
final goRouterProvider = Provider<GoRouter>((ref) {
  // ShellRoute with /history, /charts, /settings
  // Auth routes outside shell
});
```

From app/lib/features/settings/presentation/settings_screen.dart:
```dart
class SettingsScreen extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return ListView(children: [
      const SizedBox(height: 16),
      ListTile(leading: const Icon(Icons.logout), title: const Text('Log Out'), onTap: () { ... }),
    ]);
  }
}
```

Backend API (from Plan 01):
```
POST   /api/v1/categories           -- Create: {name, icon, color} -> 201 + category JSON
GET    /api/v1/categories           -- List: -> 200 + [category]
PUT    /api/v1/categories/:id       -- Update: {name, icon, color} -> 200
DELETE /api/v1/categories/:id       -- Delete: -> 204
PUT    /api/v1/categories/reorder   -- Reorder: [{id, sort_order}] -> 200
POST   /api/v1/categories/bulk      -- Bulk create: {categories: [{name, icon, color}]} -> 201 + [category]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Category data layer, state management, and reusable widgets</name>
  <files>
    app/lib/features/categories/data/models/category.dart,
    app/lib/features/categories/data/category_repository.dart,
    app/lib/features/categories/data/category_icons.dart,
    app/lib/features/categories/data/category_colors.dart,
    app/lib/features/categories/domain/category_state.dart,
    app/lib/providers/category_provider.dart,
    app/lib/features/categories/presentation/widgets/category_chip.dart,
    app/lib/features/categories/presentation/widgets/category_tile.dart,
    app/lib/features/categories/presentation/widgets/icon_picker_sheet.dart,
    app/lib/features/categories/presentation/widgets/color_picker_sheet.dart
  </files>
  <behavior>
    - Category model parses JSON from API including id, name, icon, color, sortOrder
    - CategoryRepository calls all 6 API endpoints via Dio
    - CategoryNotifier manages CategoryState lifecycle (Initial -> Loading -> Loaded/Error)
    - CategoryNotifier exposes: loadCategories, createCategory, updateCategory, deleteCategory, reorderCategories, bulkCreateStarters
    - Icon picker shows 30+ icons in a grid inside a bottom sheet, returns selected icon name string
    - Color picker shows 16 colors as circles inside a bottom sheet, returns selected Color
    - CategoryChip renders compact pill (colored background with icon + name)
    - CategoryTile renders list row (icon in colored circle + name + drag handle)
  </behavior>
  <action>
    1. Create `app/lib/features/categories/data/models/category.dart`:
       - Category class with: id, name, icon, color (all String), sortOrder (int)
       - factory Category.fromJson(Map<String, dynamic> json) -- parse sort_order from JSON
       - toJson() -> {name, icon, color} (for create/update requests, no id/sortOrder)
       - copyWith method for reorder operations

    2. Create `app/lib/features/categories/data/category_icons.dart`:
       - const Map<String, IconData> categoryIcons with 33 curated Material Icons grouped by concept (food, transport, shopping, home, health, entertainment, education, personal, finance, misc) -- use exact list from RESEARCH.md
       - Export the map for use by icon picker and category display widgets

    3. Create `app/lib/features/categories/data/category_colors.dart`:
       - const List<Color> categoryColors with 16 colors -- use exact hex values from RESEARCH.md
       - Helper: Color parseHexColor(String hex) to convert "#FF5722" to Color
       - Helper: String colorToHex(Color color) to convert Color to "#FF5722"

    4. Create `app/lib/features/categories/data/category_repository.dart`:
       - CategoryRepository with Dio dependency (from dioProvider)
       - categoryRepositoryProvider = Provider((ref) => CategoryRepository(ref.read(dioProvider)))
       - Methods: getCategories(), createCategory(name, icon, color), updateCategory(id, name, icon, color), deleteCategory(id), reorderCategories(List of {id, sortOrder}), bulkCreateCategories(List of {name, icon, color})
       - All methods hit /api/v1/categories endpoints
       - Parse responses into Category models

    5. Create `app/lib/features/categories/domain/category_state.dart`:
       - Sealed class hierarchy following auth_state.dart pattern:
         CategoryInitial, CategoryLoading, CategoryLoaded(List<Category> categories), CategoryError(String message)

    6. Create `app/lib/providers/category_provider.dart`:
       - CategoryNotifier extends StateNotifier<CategoryState>
       - Constructor takes CategoryRepository
       - Methods: loadCategories(), createCategory(name, icon, color), updateCategory(id, name, icon, color), deleteCategory(id), reorderCategories(oldIndex, newIndex), bulkCreateStarters()
       - reorderCategories: optimistically reorder local list, debounce API call (use a simple timer, 500ms)
       - bulkCreateStarters: use the 6 starter categories from research (Food, Transport, Housing, Shopping, Coffee, Entertainment)
       - categoryStateProvider = StateNotifierProvider<CategoryNotifier, CategoryState>

    7. Create `app/lib/features/categories/presentation/widgets/category_chip.dart`:
       - CategoryChip StatelessWidget: compact pill with colored background (color.withOpacity(0.15)), icon + name text in category color
       - Uses categoryIcons map to resolve icon name to IconData
       - Uses parseHexColor to resolve color string
       - Follows flat design (no elevation, no shadows)

    8. Create `app/lib/features/categories/presentation/widgets/category_tile.dart`:
       - CategoryTile StatelessWidget: ListTile with leading colored circle containing icon, title = category name, trailing = drag handle icon (Icons.drag_handle)
       - Used inside ReorderableListView in categories_screen

    9. Create `app/lib/features/categories/presentation/widgets/icon_picker_sheet.dart`:
       - showIconPicker(BuildContext context, {String? currentIcon}) async -> String?
       - Shows modal bottom sheet with GridView of icons (4-5 columns)
       - Each icon in a circle, currently selected icon has a highlight ring
       - Tapping an icon pops the sheet with the icon name string
       - Flat design -- no elevation on the sheet

    10. Create `app/lib/features/categories/presentation/widgets/color_picker_sheet.dart`:
        - showColorPicker(BuildContext context, {Color? currentColor}) async -> Color?
        - Shows modal bottom sheet with Wrap of 16 color circles
        - Currently selected color has a check mark overlay
        - Tapping a color pops the sheet with the Color value
        - Flat design -- no elevation on the sheet

    Verify compilation: `cd app && flutter analyze --no-fatal-infos`
  </action>
  <verify>
    <automated>cd /home/nnc/fm/finance-android/app && flutter analyze --no-fatal-infos</automated>
  </verify>
  <done>Category model, repository, state, provider, and all 4 reusable widgets compile cleanly. Icon map has 33 entries, color palette has 16 entries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Categories screen, form screen, routing, settings integration, and widget tests</name>
  <files>
    app/lib/features/categories/presentation/categories_screen.dart,
    app/lib/features/categories/presentation/category_form_screen.dart,
    app/lib/features/settings/presentation/settings_screen.dart,
    app/lib/core/router/app_router.dart,
    app/test/features/categories/presentation/categories_screen_test.dart,
    app/test/features/categories/presentation/category_form_screen_test.dart
  </files>
  <behavior>
    - CategoriesScreen shows ReorderableListView of CategoryTile widgets
    - CategoriesScreen shows "Get started quickly?" prompt on first visit (uses shared_preferences flag)
    - Tapping "Add these" bulk-creates 6 starter categories, "Skip" dismisses and never shows again
    - FAB (+) navigates to CategoryFormScreen in create mode
    - Tapping a CategoryTile navigates to CategoryFormScreen in edit mode (pre-filled)
    - Swiping left on a CategoryTile shows red delete background, releasing triggers confirmation dialog
    - CategoryFormScreen has name TextField, icon picker button (shows selected icon), color picker button (shows selected color)
    - Both icon and color are required -- form validates all three fields before submit
    - Submit calls createCategory (create mode) or updateCategory (edit mode), then pops back
    - Settings screen shows "Categories" ListTile above "Log Out" that navigates to /settings/categories
    - GoRouter has /settings/categories route
    - Widget tests verify: list renders categories, form validates required fields, form submits create, form submits edit
  </behavior>
  <action>
    1. Create `app/lib/features/categories/presentation/categories_screen.dart`:
       - ConsumerStatefulWidget (needs initState to load categories)
       - initState: ref.read(categoryStateProvider.notifier).loadCategories()
       - Build: switch on CategoryState -- show loading indicator, error message, or category list
       - When CategoryLoaded and categories.isEmpty and first visit (check shared_preferences for 'categories_starter_dismissed'):
         Show a Card with "Get started quickly?" text, brief description, "Add these" ElevatedButton and "Skip" TextButton
         "Add these" calls categoryNotifier.bulkCreateStarters() and sets the shared_preferences flag
         "Skip" just sets the shared_preferences flag
       - Category list: ReorderableListView.builder wrapping Dismissible widgets
         - Each item: Dismissible with direction endToStart, red background with delete icon
         - confirmDismiss callback shows AlertDialog: "Delete {name}?", "This category will be permanently deleted.", Cancel/Delete buttons
         - onDismissed calls categoryNotifier.deleteCategory(id)
         - Child: CategoryTile widget, onTap navigates to /settings/categories/edit?id={id} (or push CategoryFormScreen with category arg)
         - onReorder calls categoryNotifier.reorderCategories(oldIndex, newIndex)
       - FloatingActionButton (+) navigates to create form
       - AppBar title: "Categories"

    2. Create `app/lib/features/categories/presentation/category_form_screen.dart`:
       - ConsumerStatefulWidget accepting optional Category for edit mode
       - Form with:
         - TextFormField for name (validator: required, non-empty)
         - Icon picker row: shows currently selected icon in colored circle + "Choose icon" text, taps opens showIconPicker bottom sheet, awaits result
         - Color picker row: shows currently selected color circle + "Choose color" text, taps opens showColorPicker bottom sheet, awaits result
       - Validation: name non-empty, icon selected, color selected -- show inline error text under each if missing on submit
       - Submit button: "Create Category" or "Save Changes" depending on mode
       - On submit: call createCategory or updateCategory via provider, pop on success
       - AppBar title: "New Category" or "Edit Category"
       - Flat design throughout -- no card elevation, no shadows

    3. Update `app/lib/features/settings/presentation/settings_screen.dart`:
       - Add "Categories" ListTile with leading Icon(Icons.category) ABOVE the existing "Log Out" tile
       - onTap: context.go('/settings/categories') or context.push('/settings/categories')
       - Keep existing Log Out tile unchanged

    4. Update `app/lib/core/router/app_router.dart`:
       - Add /settings/categories route as a top-level GoRoute (outside ShellRoute) since it has its own AppBar and should not show bottom nav
       - Add /settings/categories/new route for CategoryFormScreen (create mode)
       - Add /settings/categories/edit route for CategoryFormScreen (edit mode, pass Category via extra)
       - Ensure auth redirect still works for these routes (they are not in _authRoutes so will correctly redirect to /welcome if unauthenticated)

    5. Write widget tests:
       a. `app/test/features/categories/presentation/categories_screen_test.dart`:
          - Create FakeCategoryNotifier (like FakeAuthNotifier pattern) that extends CategoryNotifier
          - Test: renders category list when loaded state has categories
          - Test: shows empty state / starter prompt when no categories
          - Test: shows loading indicator during CategoryLoading state
          - Use ProviderScope with overrides for categoryStateProvider

       b. `app/test/features/categories/presentation/category_form_screen_test.dart`:
          - Test: shows "New Category" title in create mode
          - Test: shows "Edit Category" title and pre-filled name in edit mode
          - Test: validates required name field on submit
          - Test: validates icon selection required on submit
          - Test: validates color selection required on submit

    6. Run all tests: `cd app && flutter test`
  </action>
  <verify>
    <automated>cd /home/nnc/fm/finance-android/app && flutter test</automated>
  </verify>
  <done>Categories screen renders with ReorderableListView and starter prompt. Form screen validates and submits. Settings has Categories link. Router has category routes. 8+ widget tests pass. Full flutter test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete category management flow end-to-end</name>
  <files>none</files>
  <action>
    Human verifies the complete category management system works end-to-end.
    What was built: Backend API (6 endpoints) + Flutter UI (list, create/edit form, icon picker, color picker, drag-to-reorder, swipe-to-delete, starter prompt).

    Steps to verify:
    1. Start backend: `cd server && go run ./cmd/api/` (ensure Docker DB is running)
    2. Launch Flutter app: `cd app && flutter run`
    3. Log in with an existing account (or create one)
    4. Navigate to Settings tab -- verify "Categories" row appears above "Log Out"
    5. Tap "Categories" -- verify empty state with "Get started quickly?" prompt
    6. Tap "Add these" -- verify 6 starter categories appear (Food, Transport, Housing, Shopping, Coffee, Entertainment) each with icon and color
    7. Tap the + FAB -- verify create form with name field, icon picker button, color picker button
    8. Tap icon picker -- verify bottom sheet with ~33 Material Icons in a grid, select one
    9. Tap color picker -- verify bottom sheet with 16 color circles, select one
    10. Enter name "Test Category" and tap Create -- verify new category appears in list
    11. Tap the new category -- verify edit form pre-fills name, icon, and color
    12. Change the name and save -- verify updated name in list
    13. Drag a category to reorder -- verify new order persists (close and reopen Categories screen)
    14. Swipe left on a category -- verify red delete background appears
    15. Confirm deletion in dialog -- verify category removed from list
  </action>
  <verify>User types "approved" or describes issues to fix</verify>
  <done>All 15 verification steps pass. Category CRUD, reorder, delete, starter prompt, and visual identity confirmed working.</done>
</task>

</tasks>

<verification>
```bash
# Backend tests
cd /home/nnc/fm/finance-android/server && go test ./... -v

# Flutter tests
cd /home/nnc/fm/finance-android/app && flutter test

# Flutter analysis
cd /home/nnc/fm/finance-android/app && flutter analyze --no-fatal-infos
```
All test suites green, no analysis errors.
</verification>

<success_criteria>
- Category model, repository, state, provider exist with proper Riverpod wiring
- 33 curated icons in category_icons.dart, 16 colors in category_colors.dart
- CategoriesScreen with ReorderableListView, Dismissible swipe-to-delete, starter prompt
- CategoryFormScreen with name/icon/color validation and create/edit modes
- CategoryChip reusable widget ready for Phase 6 and 7
- Icon and color pickers as bottom sheets
- Settings screen links to Categories
- GoRouter has /settings/categories routes
- 8+ widget tests passing
- Full flutter test suite green
- Human-verified end-to-end flow
</success_criteria>

<output>
After completion, create `.planning/phases/03-categories/03-02-SUMMARY.md`
</output>
