---
phase: 04-expense-entry
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - app/lib/features/expenses/data/models/expense.dart
  - app/lib/features/expenses/data/expense_repository.dart
  - app/lib/features/expenses/domain/expense_state.dart
  - app/lib/providers/expense_provider.dart
  - app/lib/features/expenses/presentation/expense_form_screen.dart
  - app/lib/core/router/app_router.dart
  - app/lib/shared/widgets/app_scaffold.dart
  - app/lib/features/history/presentation/history_screen.dart
  - app/test/features/expenses/presentation/expense_form_screen_test.dart
  - app/test/features/expenses/data/models/expense_test.dart
  - app/pubspec.yaml
autonomous: true
requirements: [EXP-01, EXP-02, EXP-05]

must_haves:
  truths:
    - "User taps FAB and is taken to a full-screen expense form"
    - "Amount field auto-focuses with numpad keyboard on form open"
    - "Category picker shows horizontal scrollable row of CategoryChips -- tap to select"
    - "Date defaults to today, tappable to open date picker"
    - "Note field is optional -- user can skip it"
    - "Tapping Save calls POST /api/v1/expenses with amount_cents (integer), category_id, note, expense_date"
    - "After save, user is popped back to previous screen with SnackBar confirmation"
    - "Amounts display with locale formatting via intl NumberFormat.currency()"
    - "Quick expense (amount + category + save) is achievable in 3 taps after FAB"
  artifacts:
    - path: "app/lib/features/expenses/data/models/expense.dart"
      provides: "Expense model with fromJson, parseAmountToCents(), formatCents()"
      contains: "class Expense"
    - path: "app/lib/features/expenses/data/expense_repository.dart"
      provides: "ExpenseRepository with createExpense and getExpenses"
      contains: "class ExpenseRepository"
    - path: "app/lib/providers/expense_provider.dart"
      provides: "ExpenseNotifier StateNotifier managing expense list state"
      contains: "class ExpenseNotifier"
    - path: "app/lib/features/expenses/presentation/expense_form_screen.dart"
      provides: "Full-screen form with amount, category chips, note, date picker"
      contains: "class ExpenseFormScreen"
    - path: "app/test/features/expenses/data/models/expense_test.dart"
      provides: "Unit tests for cents parsing and formatting"
      min_lines: 20
    - path: "app/test/features/expenses/presentation/expense_form_screen_test.dart"
      provides: "Widget tests for form rendering, validation, submission"
      min_lines: 40
  key_links:
    - from: "app/lib/shared/widgets/app_scaffold.dart"
      to: "app/lib/features/expenses/presentation/expense_form_screen.dart"
      via: "FAB onPressed navigates to /expenses/new"
      pattern: "context\\.push.*expenses/new"
    - from: "app/lib/features/expenses/presentation/expense_form_screen.dart"
      to: "app/lib/providers/expense_provider.dart"
      via: "ref.read(expenseStateProvider.notifier).createExpense()"
      pattern: "expenseStateProvider"
    - from: "app/lib/features/expenses/data/expense_repository.dart"
      to: "/api/v1/expenses"
      via: "Dio POST request"
      pattern: "dio\\.post.*expenses"
---

<objective>
Flutter expense entry UI: model, repository, state management, full-screen form with amount/category/note/date fields, FAB wiring, routing, and widget tests.

Purpose: Delivers the primary user action -- logging an expense quickly. FAB tap opens form, user enters amount + taps category + saves. Optimized for speed (under 3 seconds for quick entry).
Output: Working expense form screen, wired FAB, expense data layer, widget tests.
</objective>

<execution_context>
@/home/nnc/.claude/get-shit-done/workflows/execute-plan.md
@/home/nnc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-expense-entry/04-RESEARCH.md
@.planning/phases/04-expense-entry/04-01-SUMMARY.md
@.planning/phases/03-categories/03-02-SUMMARY.md

<interfaces>
<!-- Existing Flutter patterns to follow -->

From app/lib/features/categories/data/models/category.dart:
```dart
class Category {
  const Category({required this.id, required this.name, required this.icon, required this.color, required this.sortOrder});
  factory Category.fromJson(Map<String, dynamic> json) { ... }
  Map<String, dynamic> toJson() => { ... };
}
```

From app/lib/features/categories/data/category_repository.dart:
```dart
class CategoryRepository {
  CategoryRepository(this._dio);
  final Dio _dio;
  // Methods: getCategories, createCategory, updateCategory, deleteCategory, reorder, bulkCreate
}
```

From app/lib/providers/category_provider.dart:
```dart
class CategoryNotifier extends StateNotifier<CategoryState> {
  CategoryNotifier(this._repository) : super(const CategoryInitial());
  final CategoryRepository _repository;
}
```

From app/lib/features/categories/presentation/widgets/category_chip.dart:
```dart
// Reusable widget -- use directly in expense form for category selection
// CategoryChip(category: category, onTap: () => ..., isSelected: bool)
```

From app/lib/core/router/app_router.dart:
```dart
// Category routes outside ShellRoute pattern:
GoRoute(path: '/settings/categories', builder: ...),
GoRoute(path: '/settings/categories/new', builder: ...),
// Add expense route similarly: GoRoute(path: '/expenses/new', builder: ...)
```

From app/lib/shared/widgets/app_scaffold.dart:
```dart
// FAB exists but onPressed currently does nothing or has placeholder
// Wire: onPressed: () => context.push('/expenses/new')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expense data layer, state management, and utility functions</name>
  <files>
    app/lib/features/expenses/data/models/expense.dart,
    app/lib/features/expenses/data/expense_repository.dart,
    app/lib/features/expenses/domain/expense_state.dart,
    app/lib/providers/expense_provider.dart,
    app/test/features/expenses/data/models/expense_test.dart,
    app/pubspec.yaml
  </files>
  <behavior>
    - parseAmountToCents("12.34") returns 1234
    - parseAmountToCents("1234.56") returns 123456
    - parseAmountToCents("1,234.56") returns 123456 (strips commas)
    - parseAmountToCents("0") returns null (non-positive)
    - parseAmountToCents("abc") returns null (invalid)
    - parseAmountToCents("-5") returns null (negative)
    - parseAmountToCents("1.1") returns 110 (uses .round() not .toInt())
    - formatCents(123456) returns "$1,234.56"
    - formatCents(100) returns "$1.00"
    - formatCents(1) returns "$0.01"
    - Expense.fromJson correctly parses API response with amount_cents as int
  </behavior>
  <action>
1. Add `intl` dependency: `cd app && flutter pub add intl`

2. Create `app/lib/features/expenses/data/models/expense.dart`:
   - Expense class with: id (String), categoryId (String), amountCents (int), note (String), expenseDate (DateTime)
   - factory Expense.fromJson(Map) -- parse amount_cents as int, expense_date via DateTime.parse
   - Static utility: `static int? parseAmountToCents(String input)` -- strip commas, double.tryParse, check > 0, multiply by 100 and .round()
   - Static utility: `static String formatCents(int cents)` -- use NumberFormat.currency(locale: 'en_US', symbol: r'$').format(cents / 100)

3. Create `app/lib/features/expenses/data/expense_repository.dart`:
   - ExpenseRepository with Dio field (same as CategoryRepository)
   - `Future<Expense> createExpense({required String categoryId, required int amountCents, required String note, required String expenseDate})` -- POST /api/v1/expenses, return Expense.fromJson
   - `Future<List<Expense>> getExpenses({int limit = 50, int offset = 0})` -- GET /api/v1/expenses?limit=&offset=, return list

4. Create `app/lib/features/expenses/domain/expense_state.dart`:
   - Sealed state hierarchy mirroring CategoryState: ExpenseInitial, ExpenseLoading, ExpenseLoaded(List<Expense>), ExpenseError(String)

5. Create `app/lib/providers/expense_provider.dart`:
   - expenseRepositoryProvider: Provider creating ExpenseRepository with dioProvider
   - ExpenseNotifier extends StateNotifier<ExpenseState>
   - createExpense method: takes params, calls repo, on success prepends to loaded list (so new expense appears immediately), returns true/false
   - loadExpenses method: fetches from API, sets ExpenseLoaded
   - expenseStateProvider: StateNotifierProvider

6. Create `app/test/features/expenses/data/models/expense_test.dart`:
   - Test parseAmountToCents with all behavior cases above
   - Test formatCents with all behavior cases above
   - Test Expense.fromJson with sample API response
  </action>
  <verify>
    <automated>cd /home/nnc/fm/finance-android/app && flutter test test/features/expenses/data/models/expense_test.dart</automated>
  </verify>
  <done>Expense model with parseAmountToCents and formatCents, repository, state, provider all compile. Unit tests for cents conversion and formatting all pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expense form screen, routing, FAB wiring, and widget tests</name>
  <files>
    app/lib/features/expenses/presentation/expense_form_screen.dart,
    app/lib/core/router/app_router.dart,
    app/lib/shared/widgets/app_scaffold.dart,
    app/lib/features/history/presentation/history_screen.dart,
    app/test/features/expenses/presentation/expense_form_screen_test.dart
  </files>
  <behavior>
    - Form renders with Amount, Category, Note, and Date fields in that order
    - Amount field has auto-focus and numpad keyboard type
    - Category section shows horizontal scrollable CategoryChips from user's categories
    - Tapping a CategoryChip selects it (visual feedback)
    - Date field shows "Today" by default
    - Tapping date opens material date picker
    - Save with valid amount + selected category calls createExpense
    - Save with empty amount shows inline error
    - Save without category selected shows inline error
    - After successful save, form pops and shows SnackBar
  </behavior>
  <action>
1. Create `app/lib/features/expenses/presentation/expense_form_screen.dart`:
   - ConsumerStatefulWidget (same pattern as CategoryFormScreen)
   - AppBar title "New Expense", no bottom nav (outside ShellRoute)
   - Amount field: TextField with TextEditingController, `keyboardType: TextInputType.numberWithOptions(decimal: true)`, `autofocus: true`, prefix Text('\$'), decoration with "Amount" label
   - Category picker: Watch categoryStateProvider. If CategoryLoaded, show horizontal SingleChildScrollView with Row of CategoryChip widgets. Track selectedCategoryId in local state. If categories in initial state, trigger loadCategories in initState.
   - Note field: TextField, optional, "Note (optional)" label
   - Date field: ListTile/InkWell showing formatted date. Defaults to DateTime.now(). On tap: showDatePicker with firstDate 1 year ago, lastDate today. Track selectedDate in local state.
   - Save button: Full-width FilledButton at bottom. On press:
     a. Set _submitted = true (triggers validation display)
     b. Validate: amount not empty and parseAmountToCents returns non-null, category selected
     c. Call ref.read(expenseStateProvider.notifier).createExpense(...)
     d. On success: context.pop(), ScaffoldMessenger.showSnackBar("Expense saved")
     e. On failure: show error SnackBar
   - Inline validation: show error text under amount field if empty or invalid (only after _submitted), show "Select a category" text if none selected (only after _submitted)

2. Add route in app_router.dart:
   - GoRoute(path: '/expenses/new', builder: (context, state) => const ExpenseFormScreen()) outside ShellRoute, same level as category routes

3. Wire FAB in app_scaffold.dart:
   - Change FAB onPressed to: `() => context.push('/expenses/new')`
   - Import go_router for context.push

4. Update history_screen.dart to watch expenseStateProvider and show a simple list of expenses (name + formatted amount) as a preview. This ensures "newly logged expenses appear immediately" per success criteria. Keep it minimal -- just a ListView of expense items showing formatted cents and the expense date. If no expenses, show a centered "No expenses yet" message.

5. Create `app/test/features/expenses/presentation/expense_form_screen_test.dart`:
   - Create FakeExpenseNotifier (same pattern as FakeCategoryNotifier) for provider overrides
   - Create FakeCategoryNotifier with loaded categories for the chip picker
   - testWidgets: form renders all four fields (amount, category chips, note, date)
   - testWidgets: amount field has autofocus
   - testWidgets: date shows today's date by default
   - testWidgets: save with empty amount shows error
   - testWidgets: save without category shows error
   - testWidgets: selecting a category chip highlights it
  </action>
  <verify>
    <automated>cd /home/nnc/fm/finance-android/app && flutter test test/features/expenses/ && flutter test</automated>
  </verify>
  <done>ExpenseFormScreen renders with all fields in correct order, FAB navigates to /expenses/new, amount auto-focuses with numpad, category chips are tappable, date defaults to today, inline validation works, successful save pops + SnackBar, all widget tests pass, full Flutter test suite green</done>
</task>

</tasks>

<verification>
- `cd app && flutter test test/features/expenses/` -- all expense tests pass
- `cd app && flutter test` -- full Flutter suite green (no regressions)
- FAB in app_scaffold.dart navigates to /expenses/new
- ExpenseFormScreen field order: Amount -> Category -> Note -> Date
- Amount field has autofocus and numpad keyboard
- Date defaults to today
- History screen shows saved expenses immediately
</verification>

<success_criteria>
- User taps FAB and sees full-screen expense form
- Amount field auto-focuses with numpad
- Category chips are tappable for selection
- Quick entry path: type amount, tap chip, tap save = 3 actions after FAB
- Save persists expense via API and pops with SnackBar confirmation
- Amounts use integer cents internally, display with $ formatting
- All widget tests pass
- Full Flutter test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/04-expense-entry/04-02-SUMMARY.md`
</output>
