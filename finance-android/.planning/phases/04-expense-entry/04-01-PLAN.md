---
phase: 04-expense-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/internal/db/migrations/00004_expenses.sql
  - server/internal/db/queries/expenses.sql
  - server/internal/db/sqlc/expenses.sql.go
  - server/internal/db/sqlc/models.go
  - server/internal/db/sqlc/querier.go
  - server/internal/handler/expense.go
  - server/internal/handler/expense_db.go
  - server/internal/handler/expense_test.go
  - server/internal/router/router.go
  - server/cmd/api/main.go
autonomous: true
requirements: [EXP-01, EXP-05]

must_haves:
  truths:
    - "POST /api/v1/expenses creates an expense with amount_cents, category_id, note, expense_date and returns 201"
    - "GET /api/v1/expenses returns the authenticated user's expenses ordered by date descending"
    - "Amounts are stored and returned as integer cents (BIGINT), never floating-point"
    - "Invalid requests (missing category_id, zero/negative amount, bad date) return 400 with error message"
    - "Foreign key constraint prevents expenses referencing non-existent categories"
  artifacts:
    - path: "server/internal/db/migrations/00004_expenses.sql"
      provides: "expenses table with BIGINT amount_cents, DATE expense_date, FK to categories"
      contains: "CREATE TABLE expenses"
    - path: "server/internal/handler/expense.go"
      provides: "ExpenseDB interface, ExpenseHandler with Create and List methods"
      exports: ["ExpenseDB", "ExpenseHandler", "NewExpenseHandler"]
    - path: "server/internal/handler/expense_db.go"
      provides: "PgExpenseDB implementing ExpenseDB via sqlc"
      contains: "PgExpenseDB"
    - path: "server/internal/handler/expense_test.go"
      provides: "Handler unit tests with mock ExpenseDB"
      min_lines: 80
  key_links:
    - from: "server/internal/handler/expense.go"
      to: "server/internal/handler/expense_db.go"
      via: "ExpenseDB interface implementation"
      pattern: "type PgExpenseDB struct"
    - from: "server/internal/router/router.go"
      to: "server/internal/handler/expense.go"
      via: "Route registration in protected group"
      pattern: "expenses\\.POST|expenses\\.GET"
---

<objective>
Go backend for expense entry: database migration, sqlc queries, ExpenseDB interface, ExpenseHandler with Create and List endpoints, and unit tests.

Purpose: Provides the API that the Flutter expense form (Plan 02) will call to persist expenses. Follows the exact CategoryDB/CategoryHandler pattern from Phase 3.
Output: Two new API endpoints (POST/GET /api/v1/expenses), expenses table, handler tests.
</objective>

<execution_context>
@/home/nnc/.claude/get-shit-done/workflows/execute-plan.md
@/home/nnc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-expense-entry/04-RESEARCH.md
@.planning/phases/03-categories/03-01-SUMMARY.md

<interfaces>
<!-- Existing Go patterns the executor needs to follow exactly -->

From server/internal/handler/category.go (mirror this pattern):
```go
type CategoryDB interface {
    CreateCategory(userID, name, icon, color string) (MockCategory, error)
    GetCategoriesByUser(userID string) ([]MockCategory, error)
    UpdateCategory(id, userID, name, icon, color string) error
    DeleteCategory(id, userID string) error
    ReorderCategories(userID string, items []ReorderItem) error
    BulkCreateCategories(userID string, items []BulkItem) ([]MockCategory, error)
}

type CategoryHandler struct {
    db CategoryDB
}

func NewCategoryHandler(db CategoryDB) *CategoryHandler {
    return &CategoryHandler{db: db}
}
```

From server/internal/router/router.go (add expense routes to protected group):
```go
func Setup(db handler.AuthDB, categoryDB handler.CategoryDB, authSvc *service.AuthService) *gin.Engine {
    // ... protected group with middleware.AuthMiddleware ...
    protected := api.Group("/")
    protected.Use(middleware.AuthMiddleware(authSvc.JWTSecret()))
}
```

From server/cmd/api/main.go (wire PgExpenseDB into router):
```go
// Pattern: create PgXxxDB with pool, pass to router.Setup
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expenses migration, sqlc queries, ExpenseDB interface, and PgExpenseDB</name>
  <files>
    server/internal/db/migrations/00004_expenses.sql,
    server/internal/db/queries/expenses.sql,
    server/internal/handler/expense.go,
    server/internal/handler/expense_db.go
  </files>
  <behavior>
    - CreateExpense with valid fields returns expense with generated UUID and timestamps
    - CreateExpense with non-existent category_id fails (FK constraint)
    - GetExpensesByUser returns expenses for a specific user ordered by expense_date DESC, created_at DESC
    - GetExpensesByUser with limit/offset paginates correctly
  </behavior>
  <action>
1. Create migration `server/internal/db/migrations/00004_expenses.sql` exactly as specified in RESEARCH.md:
   - expenses table with id UUID PK, user_id FK to users ON DELETE CASCADE, category_id FK to categories ON DELETE RESTRICT, amount_cents BIGINT NOT NULL CHECK (amount_cents > 0), note TEXT NOT NULL DEFAULT '', expense_date DATE NOT NULL DEFAULT CURRENT_DATE, created_at/updated_at TIMESTAMPTZ
   - Indexes: idx_expenses_user_id, idx_expenses_user_date (composite)
   - goose Up/Down markers

2. Create sqlc queries `server/internal/db/queries/expenses.sql`:
   - CreateExpense: INSERT returning all columns. Parameters: user_id, category_id, amount_cents, note, expense_date.
   - GetExpensesByUser: SELECT with WHERE user_id = $1 ORDER BY expense_date DESC, created_at DESC LIMIT $2 OFFSET $3.

3. Run `cd server && sqlc generate` to regenerate Go code.

4. Create `server/internal/handler/expense.go`:
   - MockExpense struct mirroring sqlc-generated model (ID, UserID, CategoryID, AmountCents int64, Note, ExpenseDate, CreatedAt, UpdatedAt)
   - ExpenseDB interface with: CreateExpense(userID, categoryID string, amountCents int64, note string, expenseDate time.Time) (MockExpense, error) and GetExpensesByUser(userID string, limit, offset int) ([]MockExpense, error)
   - ExpenseHandler struct with db ExpenseDB field
   - NewExpenseHandler(db ExpenseDB) constructor

5. Create `server/internal/handler/expense_db.go`:
   - PgExpenseDB struct with queries field (sqlc Querier)
   - NewPgExpenseDB constructor
   - Implement CreateExpense: parse UUIDs, call sqlc CreateExpense, convert sqlc model to MockExpense
   - Implement GetExpensesByUser: call sqlc query with int32 limit/offset, convert results
   - Handle expense_date as time.Time DATE-only (use time.Parse("2006-01-02", ...))
  </action>
  <verify>
    <automated>cd /home/nnc/fm/finance-android/server && sqlc generate && go build ./...</automated>
  </verify>
  <done>Migration file exists, sqlc generates without errors, ExpenseDB interface and PgExpenseDB compile, expense.go and expense_db.go follow CategoryDB pattern exactly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExpenseHandler Create/List endpoints, route wiring, and unit tests</name>
  <files>
    server/internal/handler/expense.go,
    server/internal/handler/expense_test.go,
    server/internal/router/router.go,
    server/cmd/api/main.go
  </files>
  <behavior>
    - POST /expenses with valid body returns 201 with expense JSON (amount_cents as integer)
    - POST /expenses with missing category_id returns 400
    - POST /expenses with amount_cents <= 0 returns 400
    - POST /expenses with invalid expense_date format returns 400
    - POST /expenses with empty note succeeds (note is optional)
    - POST /expenses defaults expense_date to today if omitted
    - GET /expenses returns array of user's expenses ordered by date DESC
    - GET /expenses respects limit/offset query params (default limit=50, offset=0)
  </behavior>
  <action>
1. Add Create method to ExpenseHandler in expense.go:
   - Request struct: createExpenseRequest with CategoryID (required), AmountCents int64 (required), Note string, ExpenseDate string
   - Bind JSON, validate: category_id required, amount_cents > 0
   - Parse expense_date: if empty default to time.Now(), otherwise parse "2006-01-02" format. Return 400 on bad format.
   - Get userID from gin context (set by AuthMiddleware, key "userID")
   - Call db.CreateExpense, handle errors (check for FK violation -> 400 "invalid category")
   - Return 201 with expense JSON. Format expense_date as "2006-01-02" string in response.
   - Response includes: id, user_id, category_id, amount_cents (int64), note, expense_date, created_at

2. Add List method to ExpenseHandler in expense.go:
   - Parse limit/offset from query params (default limit=50, offset=0)
   - Get userID from context
   - Call db.GetExpensesByUser
   - Return 200 with JSON array

3. Create expense_test.go with mockExpenseDB:
   - mockExpenseDB struct implementing ExpenseDB (same pattern as mockCategoryDB in category_test.go)
   - TestCreateExpense_Success: valid request -> 201 + correct JSON shape
   - TestCreateExpense_MissingCategoryID: no category_id -> 400
   - TestCreateExpense_InvalidAmount: amount_cents=0 -> 400, amount_cents=-100 -> 400
   - TestCreateExpense_InvalidDate: bad date format -> 400
   - TestCreateExpense_DefaultDate: no expense_date -> uses today
   - TestCreateExpense_AmountCents: verify amount returned as integer (not float)
   - TestListExpenses_Success: returns array of expenses
   - Use httptest.NewRecorder + gin test context, same as category_test.go

4. Wire routes in router.go:
   - Add expenseDB handler.ExpenseDB parameter to Setup function
   - Create expenseHandler := handler.NewExpenseHandler(expenseDB) in protected group
   - Register: expenses.POST("", expenseHandler.Create) and expenses.GET("", expenseHandler.List)

5. Wire in main.go:
   - Create PgExpenseDB with pool, pass to router.Setup
  </action>
  <verify>
    <automated>cd /home/nnc/fm/finance-android/server && go test ./internal/handler/ -run TestExpense -v && go test ./... -count=1</automated>
  </verify>
  <done>All expense handler tests pass, full Go test suite green, router wires expense endpoints in protected group, main.go creates PgExpenseDB</done>
</task>

</tasks>

<verification>
- `cd server && go test ./internal/handler/ -run TestExpense -v` -- all expense tests pass
- `cd server && go test ./... -count=1` -- full suite green (no regressions)
- `cd server && go build ./...` -- compiles cleanly
- Migration file exists at server/internal/db/migrations/00004_expenses.sql
</verification>

<success_criteria>
- POST /api/v1/expenses creates expense with integer cents, returns 201
- GET /api/v1/expenses returns user's expenses by date DESC
- Invalid input (missing fields, bad amounts, bad dates) returns 400
- All handler tests pass with mock DB
- Full Go test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/04-expense-entry/04-01-SUMMARY.md`
</output>
